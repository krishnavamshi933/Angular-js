name: AngularJS.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Check AWS CLI installation
        id: check_aws_cli
        run: |
          if ! command -v aws &> /dev/null; then
            echo "::set-output name=installed::false"
          else
            echo "::set-output name=installed::true"
          fi

      - name: Install or update AWS CLI
        if: steps.check_aws_cli.outputs.installed == 'false'
        run: |
          curl "https://d1vvhvl2y92vvt.cloudfront.net/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
          unzip awscliv2.zip
          sudo ./aws/install

      - name: Install dependencies
         run: npm install


      - name: Build
        run: |
          npm run build --if-present

      - name: Set up AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1

      - name: Deploy to S3
        run: |
          aws s3 sync build/ s3://krishnavamshi.xyz/
